server:
  port: 9192

spring:
  h2:
    console:
      enabled: true
  application:
    name: Z-Order-Service
      

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost